change: css-track-expansion
created: 2026-08-21
artifact_store: hybrid

session_preflight:
  execution_mode: auto
  artifact_store: hybrid
  delivery_strategy: ask-on-risk
  review_budget_lines: 800
  chain_strategy: stacked-to-main
  note: >
    The store was RAISED to `hybrid` after planning, at the instructor's request.
    History worth keeping, because it changed a decision: during planning the
    orchestrator believed Engram was down and told every phase so. That was
    wrong. What dropped was this session's MCP tool bridge
    (`plugin:engram:engram` disconnected, so `mem_save`/`mem_search` were not
    callable). Engram itself never stopped: `engram serve` was running the whole
    time, the CLI at ~/.local/bin/engram v1.20.0 was available, and it supports
    `--topic` for canonical topic keys. The consequence of that false premise was
    real — the preflight menu offered only file/inline-safe stores, so `hybrid`
    (used by the three earlier changes in this repo) was never on the table, and
    the instructor caught it. The four phase artifacts were written to Engram
    afterwards through the CLI under their canonical topic keys, so both backends
    now hold this change. LESSON: if the Engram MCP bridge drops, use the CLI —
    do not declare Engram down.

product_input:
  source: /home/diegobonilla/proyectos/cssGame/plan-mejoras-css.md
  origin: >
    Written by the orchestrator after reviewing the complete W3Schools CSS
    sidebar (~75 topic groups, ~250 pages) against our own track, then reviewed
    and steered by the instructor. It is the source of intent for this change.
  measured_baseline:
    css_modules: 25
    css_exercises: 220
    css_properties_taught: 78
  instructor_decisions:
    - Grow by ADDING modules and GROUPING them (not by deepening the existing 25).
    - Start with Fase 0 (regroup) and Fase 1 (the five most-missed modules).
    - Sticky as a full step in class 9 (unrelated to this change; recorded for context).
  anti_plagiarism_constraint: >
    What was taken from W3Schools is the topic INVENTORY, which is fact. No text,
    no examples, no literal ordering. Every example must be authored by us, and
    preferably grounded in the running product (Ana Martinez's CV). This is
    carried as a requirement in css-track-content/spec.md, not as a note.
  rejected_by_policy: >
    W3Schools' recipe collection (tooltips, modals, sprites, pagination,
    dropdowns, galleries) is out of scope BY POLICY, not by oversight: it teaches
    pasting, not concepts. If any of it earns a place it enters as a `reto`.

phases:
  propose:
    status: done
    engram_topic_key: sdd/css-track-expansion/proposal
    engram_observation: 262
    artifact: openspec/changes/css-track-expansion/proposal.md
    orchestrator_verified:
      - >
        CORRECTED A FALSE PREMISE THE ORCHESTRATOR HAD PUT IN THE PLAN:
        `src/data/modules/index.ts` ends with `.sort((a, b) => a.order - b.order)`,
        so the app DOES sort by `order`. TWO stale comments claim the opposite —
        `index.ts:125` ("el orden de este array ES el orden del track: nada
        ordena por el campo `order`") and the docstring of
        `orden-curriculum-css.test.ts`. Both must be fixed by this change: a
        wrong comment about ordering is how this trap gets re-set.
      - >
        `validation.type: "includes"` is FORBIDDEN for CSS exercises by
        `validacion-curriculum.test.ts` (the token `row` matched inside `arrow`
        and `browser`). New CSS exercises use css-rules, html-structure, quiz or
        drag-drop.
      - >
        `acentuacion.test.ts` REQUIRES correct Spanish accents; it fails on a
        missing tilde or `ñ`. css-dojo/CLAUDE.md said "no accented characters"
        and was wrong — the orchestrator had written that line itself and has
        corrected it. Note the opposite rule applies to the wdevch2/ class
        materials, which are deliberately ASCII.
      - >
        `src/lib/cssRules.ts` `normalizarSelectores` only lowercases and
        collapses whitespace, so selector keys are literal:
        `[href^="https"]` and `[href^='https']` are DIFFERENT keys. Authoring
        trap for the attribute-selectors module; the spec pins one quote style.
      - >
        `src/lib/models/ModuleSettings.ts` — no document means blocked, so every
        new slug is invisible per cohort until a teacher enables it.
  spec:
    status: done
    engram_topic_key: sdd/css-track-expansion/spec
    engram_observation: 263
    artifacts:
      - openspec/changes/css-track-expansion/specs/css-track-sections/spec.md
      - openspec/changes/css-track-expansion/specs/css-track-content/spec.md
    orchestrator_verified:
      - >
        The six generic categories (intro, intermediate, advanced,
        preprocessors, frameworks, project) are used by EXACTLY the 25 CSS
        module files and no other track — checked category by category. Removing
        them from `ModuleCategory` cannot break HTML, JS, React or Next.
      - >
        `categorias-panel.test.ts:84-88` declares
        `CLOSING_CATEGORY: Partial<Record<DojoType, ModuleCategory>>` with the
        literal `css: "project"`. Removing that category BREAKS TYPECHECK there.
        The proposal had called this file "unchanged" and was wrong.
      - >
        `tipos-ejercicio.test.ts:38` asserts `sinEscribir.length <= 63`, so every
        new module must ship at least one live-editor or visual-match exercise or
        the suite regresses.
    resolved_by_orchestrator:
      - >
        SECTION ORDER: `css-caja` goes BEFORE `css-texto`. The plan's table had
        it the other way. Not a free choice: `orden-curriculum-css.test.ts` pins
        `box-model` before `unidades-css`, and that pedagogical decision was
        already made (unidades-css uses padding/margin as its examples 45 times).
        The plan file was corrected.
      - >
        INTRA-SECTION ORDER: inside `css-caja` it is box-model -> unidades-css ->
        dimensiones -> math-functions. Same reason: the guard
        `unidades-css viene antes de tipografias y dimensiones` pins it. The plan
        file was corrected. Neither of these was escalated to the instructor
        because each had exactly one coherent answer.
  design:
    status: done
    engram_topic_key: sdd/css-track-expansion/design
    engram_observation: 264
    artifact: openspec/changes/css-track-expansion/design.md
    decisions:
      - >
        Category migration in three individually-green commits: widen the union
        and append the css-* to DOJO_CATEGORY_ORDER.css -> flip the 25 category
        lines and the guards -> delete the six generic members, with typecheck as
        the proof nothing still references them.
      - >
        Renumbering happens TWICE (a relative 25-module sequence in slice 2, the
        final 1..30 in slice 3e). Appending the new modules at 26..30 and
        renumbering later is not merely ugly, it is RED: `categorias-panel.test.ts:98`
        asserts the capstone outranks every content module, so proyecto-cv-css at
        25 with math-functions at 26 fails.
      - >
        The guard rewrite replaces the module-count pin AND the slug ledger with
        one pinned 30-slug SECUENCIA asserted three ways — strictly stronger than
        what it replaces. Deriving the sequence from `order` itself was rejected
        as tautological.
      - Authoring template is DOCUMENTATION; the six existing guards are already the enforcement layer.
    orchestrator_verified:
      - >
        Both renderers skip empty categories —
        `(app)/modulos/page.tsx:80` and `(teacher)/teacher/modulos/page.tsx:378`
        both do `if (catModules.length === 0) return null;`. This is what makes a
        staged category migration invisible on screen at every intermediate
        commit, and therefore safely deployable half-done.
      - >
        A blocked module renders as a VISIBLE greyed locked card
        (`opacity-50 cursor-not-allowed`, `(app)/modulos/page.tsx:98-103`), it is
        NOT hidden. The spec's Scenario 8.1 said "MUST NOT appear" and is wrong.
        Real consequence: five locked cards become visible to every cohort at
        deploy, before enabling. This is a rollout decision, not a surprise.
      - >
        A THIRD guard breaks on removing the generic categories, beyond the two
        already known: `tipos-ejercicio.test.ts:58` reads
        `m.category === "preprocessors"` and then asserts
        `expect(preprocesadores.length).toBeGreaterThan(0)` — removing the member
        is a TS2367 no-overlap error AND turns the assert into `0 > 0`. The fix
        pins the two Sass SLUGS, deliberately not `css-herramientas`: that
        category also holds Bootstrap and Tailwind, so mapping to it would pass
        the test while silently changing what the test means.
      - "`var(--` appears nowhere in 16-css-grid.ts, so the hard-failing exercise ledger test is safe across the renumbering."
    open_risk_carried_to_apply: >
      HIGH — the forward-reference ledger moves. media-queries shifts 17 -> 25
      and transiciones-animaciones 18 -> 23, and both ledger tests key off
      `posicionDe(...)`, so existing content gets re-classified (@media in
      18/19, `transition:` in 20, typography properties in 05). Estimated +2 to
      +4 against a ceiling whose current true value is bounded, not pinned. The
      design phase had no shell, so `sdd-apply` owns this measurement
      (tasks 2.3/2.4). MEASURE BEFORE AND AFTER slice 2.
  tasks:
    status: done
    engram_topic_key: sdd/css-track-expansion/tasks
    engram_observation: 265
    artifact: openspec/changes/css-track-expansion/tasks.md
    summary: 40 tasks across 7 slices (1 sections, 1 renumber, 5 content modules).
    review_workload_forecast:
      estimated_changed_lines: "~3500-4300"
      per_slice_lines:
        slice_1_sections: "~110-140"
        slice_2_renumber_and_guard: "~180-240"
        slice_3a_to_3e_each_module: "~650-790"
      budget_risk: high
      chained_prs_recommended: true
      decision_needed_before_apply: true
    guard_outcome: >
      Review Workload Guard TRIGGERED. ~3500-4300 lines against an 800-line
      budget, chained PRs required rather than optional. Under
      delivery_strategy `ask-on-risk` the orchestrator stopped and asked the
      instructor, who chose `stacked-to-main`: the 7 PRs merge to main in
      sequence. Rationale recorded from the instructor's workflow — each slice
      merges, gets redeployed, and its module is enabled per cohort, and slice 1
      changes nothing on screen so main is safe from the first merge.
    guard_rewrite_tasks:
      - "Task 1.6 — categorias-panel.test.ts CLOSING_CATEGORY.css -> \"css-proyecto\""
      - "Task 1.7 — tipos-ejercicio.test.ts:58 slug-pinned Sass check, NOT category-based"
      - "Tasks 1.8 + 2.2 — orden-curriculum-css.test.ts: interim name swap, then full rewrite"

  apply:
    status: done
    slice_1_sections:
      status: done
      branch: feat/css-secciones
      merged: false
      commits:
        - "paso A: agregar los 9 miembros css-*, CATEGORY_META y el orden (additive)"
        - "paso B: voltear las 25 categorias, los 3 guards, los 6 order y el ledger"
        - "paso C: borrar las 6 genericas, con `tsc --noEmit` como prueba"
      gates: "npm run test:run (18 archivos / 170 tests), npx tsc --noEmit, npm run lint (0 errores / 49 warnings preexistentes), npm run build -- los cuatro verdes"
      changed_lines: 213
      forecast_was: "110-140"
      measured_by_apply:
        - >
          EL SLICE 1 SI TOCA `order`, contra design.md D6 ("touches no order").
          El test `la categoria no contradice el orden` arma el walk agrupando
          por ORDEN_CATEGORIAS y compara la POSICION contra el campo `order`.
          Con las secciones css-* y los numeros viejos daba 6 inconsistencias.
          La categoria y el `order` son UN solo hecho, no dos: no existe un
          slice 1 verde que no los mueva. Se movieron los 6 minimos
          (dimensiones 6, tipografias 7, transiciones 17, variables 18,
          shadows 19, media-queries 20), que son los valores finales de D2, asi
          que no hay trabajo tirado.
        - >
          Estime 9 inconsistencias antes de medir y eran 6. El orden
          INTRA-seccion lo deriva el test del ARRAY (ya ordenado por `order`),
          no de la tabla de la spec Req 3, y por eso `css-layout` paso intacto.
          La tarea 2.1 todavia tiene que reordenar `css-layout` y `css-visual`
          para que coincidan con Req 3.
        - >
          EL TECHO DEL LEDGER NO ESTABA "ACOTADO PERO NO FIJADO": ERA EXACTO.
          Valor real en main medido con shell = 24, contra
          `toBeLessThanOrEqual(24)`. Cero margen. El riesgo alto que D4 dejaba
          para el apply era peor de lo que estimaba: cualquier renumeracion lo
          rompia.
        - >
          Con los 6 `order` movidos el ledger dio 26. Neto: sale 1 caso
          (media-queries/`transition:`, porque media-queries se va a 20) y
          entran 3: `@media` en 18-transiciones-animaciones (x2), `@media` en
          19-variables-css (x1) y `text-align:` en 05-dimensiones (x1).
        - >
          EL PATRON `@media` DEL LEDGER ERA DEMASIADO GRUESO y eso invalidaba
          la decision de D4. Los dos casos nuevos de @media son
          `prefers-reduced-motion` (accesibilidad, dentro de animaciones) y
          `prefers-color-scheme: dark` (tema, dentro de variables): no son
          referencias adelantadas al modulo responsive, se ensenan donde estan.
          Borrarlos, como mandaba D4 al pie de la letra, habria quitado los dos
          mejores ejemplos de esos modulos.
      instructor_decision: >
        Consultada, no inventada. Se afina el patron a
        `@media[^{]*\b(min-width|max-width|min-height|max-height|orientation)\b`,
        con UNA sola definicion compartida por los dos tests que miran @media
        (el que MIDE y el que FALLA), para que no puedan decir cosas distintas.
        Ledger medido despues: 23, y CERO casos de @media, o sea que ningun
        modulo usa una media query responsive antes de tiempo. El techo baja de
        24 a 23: un techo flojo por la cantidad exacta de una regresion no es un
        techo. Rechazadas: borrar contenido (D4 literal), borrar solo el
        text-align (quedaba en 25, rojo) y subir el techo (el comentario del
        propio test dice que nunca se sube).
      ledger_measurements:
        main_baseline: 24
        after_6_order_moves: 26
        after_pattern_refinement: 23
        ceiling_before: 24
        ceiling_after: 23
        note: >
          El slice 2 mueve mas `order` (float-display 12, posicionamiento 13,
          flexbox 14, css-grid 15, propiedades-logicas 16). Eso ACERCA los
          destinos de flexbox/css-grid, asi que el conteo solo puede bajar:
          `propiedades-logicas` sale de la ventana de `display:flex`. Estimado
          22. Volver a medir en 2.3/2.4 igual, con el numero real.
      bounded_review:
        lineage: review-7f5c406f85e93bc9
        risk: medium
        lenses: ["review-reliability"]
        correction_budget: 107
        receipt: "sha256:402f9334479b4e79db931757154aa81cc9400acab800ce82ea4d20719fe73d03"
        gates: "pre-commit allow, pre-push allow (con --base-ref origin/main), pre-pr allow"
        inspection: "completed, 31 de 31 rutas"
        findings: >
          Dos, ninguno bloqueante, agendados como tareas 2.6 y 2.7.
          WARNING (deterministic, introduced): el patron MEDIA_RESPONSIVE no
          matchea la sintaxis de rango moderna `@media (width >= 48rem)`, que el
          patron viejo si agarraba. Es un bug real que introduje: el guard quedo
          mas debil en ese eje. Hoy no hay casos, el ledger mide cero @media.
          SUGGESTION: el requisito 2 (nueve colores distintos) no tiene asercion
          ejecutable, vive en un comentario.
        confirmed_by_reviewer: >
          El revisor recomputo la secuencia completa de `order` desde los 25
          parches y confirmo 1..25 contiguo sin huecos ni duplicados; que el
          array de index.ts concuerda elemento por elemento; que el indice de
          categoria es monotono no decreciente sobre esa secuencia y que
          proyecto-cv-css cierra; que los nueve colores son distintos entre si; y
          que el cambio de tipos-ejercicio.test.ts es ESTRICTAMENTE MAS FUERTE
          que el filtro por categoria que reemplazo.
        lecciones_de_proceso: >
          Se gastaron dos corridas de revisor por errores del orquestador, no del
          provider. (1) Se le paso al revisor una receta de git en lugar de la
          evidencia: el rol de revisor de claude-code NO tiene herramientas y
          solo admite la evidencia que viene en el prompt. (2) Se reuso un handle
          `repository-context` del candidato anterior. EL COMANDO CORRECTO es
          `gentle-ai review lens-context --repository-context <handle>
          --lens <lente> --delivery provider_command`, que emite el prompt
          completo y canonico, y el handle CADUCA con el candidato: si cambia el
          candidato hay que sacar el nuevo de la transicion `collect`.
          Aparte, `review status` NO acepta `--committed-only` (si `--base-ref`),
          y `review validate --gate=pre-push` necesita `--base-ref origin/main`
          cuando la rama no tiene upstream configurado.
        limite_de_capacidad: >
          El contexto del revisor acepta 32 rutas como maximo y el slice 1
          original tenia 33. El provider congela el candidato ANTES de validar
          esa capacidad, asi que el primer linaje (review-c329f7b19492b56e)
          quedo abierto e inservible. Se resolvio separando los artefactos SDD
          del codigo: 31 rutas de codigo entran. Este techo SOLO muerde en el
          slice 1, que es el unico que toca los 25 archivos de modulo -- el
          slice 2 son 27 rutas y los 3a-3e menos.
      pending_before_pr: >
        No se hizo push ni PR: el instructor decide cuando. Falta tambien el
        rollout, que en este slice es solo redeploy (no hay slug nuevo, asi que
        no hay nada que habilitar por cohorte).
    forward_to_apply:
      delivery_strategy: ask-on-risk
      chain_strategy: stacked-to-main
      pr_boundary: one-pr-per-slice
      size_exception: not-required
      review_budget_lines: 800
      strict_tdd: false
      test_command: "npm run test:run"
      gates: ["npm run test:run", "npx tsc --noEmit", "npm run lint", "npm run build"]
      merge_order: >
        1 sections -> 2 renumber+guard -> 3a math-functions (clamp) ->
        3b advanced-text -> 3c attribute-selectors -> 3d lists-and-tables ->
        3e transforms. Merge order equals revert order.
      note: >
        Start with slice 1: it is the cheapest, changes nothing on screen, and
        its three guard fixes are what make every later slice possible. Slice 2
        is a hard dependency for all of 3a-3e — a mistake there blocks every
        subsequent PR. Each of 3a-3e sits at 650-790 lines against an 800 budget,
        so any scope creep inside a single module file breaches it.
      rollout_per_content_slice: >
        merge -> redeploy on Coolify -> enable the new slug per cohort in
        /teacher/modulos. NEVER enable before redeploy. Until enabled, the module
        shows as a greyed locked card to every cohort (this is visible, not hidden).
    slice_2_renumber:
      status: done
      branch: feat/css-renumeracion
      merged: true
      gates: "173 tests, tsc, lint 0 errores, build -- los cuatro verdes"
      changed_lines: 154
      changed_paths: 11
      bounded_review:
        lineage: review-a7b1b8bcd83cff67
        risk: medium
        lenses: ["review-reliability"]
        findings: "NINGUNO"
        inspection: "completed, 11 de 11 rutas"
        gates: "pre-commit allow, pre-pr allow, pre-push allow (con --base-ref origin/main)"
      ledger_measurements:
        before: 23
        after: 23
        ceiling: 23
        note: >
          Neto cero. Sale `propiedades-logicas` de la ventana de `display:flex`
          (14 -> 16) y entra `shadows-gradients-filters` con `transition:` x5
          (19 -> 17, ahora un modulo antes de transiciones). El ensanche del
          regex no cambio el conteo.
      guard_probado: >
        El guard reescrito MUERDE, verificado a proposito: intercambiar flexbox y
        css-grid dentro de `css-layout` hace fallar tres aserciones. El guard que
        reemplaza pasaba ese mismo cambio en verde -- los 170 tests daban identico
        antes y despues de mover los 8 numeros del slice 2.
      hallazgos_del_slice_1_cerrados: >
        2.6 y 2.7 hechas. El revisor confirmo por su cuenta que el regex
        corregido es un SUPERCONJUNTO ESTRICTO del anterior: `\bwidth\b` sigue
        matcheando `min-width` porque el guion no es caracter de palabra, asi que
        no se perdio ninguna forma detectada antes, y ademas entra la sintaxis de
        rango. Verificado tambien a mano, caso por caso, incluyendo que
        prefers-reduced-motion, prefers-color-scheme y print siguen afuera.
    slice_3a_math_functions:
      status: done
      branch: feat/css-math-functions
      merged: true
      gates: "173 tests, tsc, lint 0 errores, build, 5 E2E -- los CINCO verdes"
      changed_lines: 498
      changed_paths: 22
      forecast_was: "650-790"
      rollout_pendiente: >
        ESTE SLICE AGREGA EL SLUG `math-functions`. Secuencia obligatoria:
        merge -> redeploy manual en Coolify -> habilitar por cohorte en
        /teacher/modulos. Habilitar antes del redeploy es IMPOSIBLE, el panel solo
        puede togglear slugs presentes en el ALL_MODULES desplegado. Hasta que se
        habilite se ve como TARJETA GRIS CON CANDADO para todas las cohortes.
      bounded_review:
        lineage: review-d92af024c0c35633
        risk: medium
        lenses: ["review-reliability"]
        inspection: "completed, 22 de 22 rutas"
        findings: "1 SUGGESTION, ningun blocker"
        gates: "pre-commit allow, pre-pr allow, pre-push allow"
      hallazgo_y_respuesta: >
        SUGGESTION: los tres `targetCSS` con funciones multi-argumento no tienen
        asercion DENTRO del candidato. Tenia razon sobre el alcance. El camino de
        parseo SI esta cubierto por `validacion-curriculum.test.ts`, que no cambia
        y por eso el revisor no podia verlo. Medido con el grader real: 100 en
        identico, sin espacio tras comas, espacios extra, mayusculas y orden
        inverso de reglas. 67/100/75 solo al sacar los espacios del guion, que es
        CSS INVALIDO -- resultado correcto, no defecto.
      confirmado_por_el_revisor: >
        Los 19 desplazamientos uno por uno; `order` 7..26 contiguos sin huecos ni
        duplicados; `index.ts` y la SECUENCIA sincronizados; ids y ordenes de
        lecciones (26-leccion-01..04) y ejercicios (26-ej-01..08) unicos; cada
        respuesta de quiz coincide con su opcion `isCorrect`; el mapa del drag-drop
        coincide con cada `correctZone` y cada `dropZone` declarada; y la prosa de
        las lecciones no ensena nada falso sobre el comportamiento observable.
      ledger_measurements:
        before: 23
        after: 23
        ceiling: 23
        aporte_del_modulo_nuevo: 0
      techos_sin_margen: >
        DOS techos estan exactamente en su valor y no tienen ni un caso de margen:
        el ledger en 23, y el `sinEscribir` de tipos-ejercicio en 63. Consecuencia
        para 3b-3e: cada modulo nuevo DEBE traer al menos un live-editor o
        visual-match, y NO puede usar `line-height`, `font-family`, `text-align`,
        `@media` con width, `display:flex`, `display:grid`, `var(--`,
        `transition:`, `box-shadow` ni `linear-gradient`, porque todos se ensenan
        despues y cada aparicion suma un caso al ledger.
      no_cubierto_por_la_revision: >
        Si el contenido se entiende y si el nivel le sirve a la cohorte. Los tests
        prueban que parsea, que puntua 100, que tiene tildes y que no adelanta
        conceptos. No prueban que se entienda. Eso lo lee el instructor.
    slice_3b_advanced_text:
      status: done
      branch: feat/css-advanced-text
      merged: true
      gates: "173 tests, tsc, lint 0 errores, build, 5 E2E -- los CINCO verdes"
      changed_lines: 493
      changed_paths: 21
      rollout_pendiente: >
        AGREGA EL SLUG `advanced-text`. merge -> redeploy en Coolify -> habilitar
        por cohorte en /teacher/modulos. Hasta habilitarlo se ve como tarjeta gris
        con candado para todas las cohortes.
      bounded_review:
        lineage: review-ada5775b076e2cb4
        risk: medium
        lenses: ["review-reliability"]
        inspection: "completed, 21 de 21 rutas"
        findings: "NINGUNO"
        gates: "pre-commit allow, pre-pr allow, pre-push allow"
      ledger_measurements:
        before: 23
        after: 23
        ceiling: 23
        aporte_del_modulo_nuevo: 0
      dato_util_para_3c_a_3e: >
        `text-align`, `line-height` y `font-family` dejan de ser referencias
        adelantadas para cualquier modulo en slot >= 9, porque el ledger los apunta
        a `tipografias`, que queda fijo en el 8. Y `text-shadow` NUNCA cuenta: el
        patron del ledger es `\bbox-shadow`, que es otra palabra.
      commits_partidos: >
        El slice va en dos commits a proposito: primero el archivo del modulo SIN
        registrar (verde e invisible, ningun guard lo ve si no esta en ALL_MODULES)
        y despues la mecanica de insercion (18 shifts + index.ts + SECUENCIA, 41
        lineas). Eso deja disponible la opcion de revisar solo la mecanica en 3c-3e.
        En 3b el instructor eligio revisar el candidato COMPLETO.
      confirmado_por_el_revisor: >
        Los 18 desplazamientos uno por uno; `order` 9..27 contiguos sin huecos ni
        duplicados; index.ts y la SECUENCIA sincronizados; 4 lecciones con ids y
        ordenes unicos y 8 ejercicios idem; cada validacion consistente con su tipo.
        Ademas, sin que se le pidiera: que `box-model` y `unidades-css`, ausentes
        del manifiesto, no quedaran con un `order` en colision; y un escaneo del
        archivo por backticks sin escapar e interpolaciones accidentales.

  defecto_abierto_del_guard_de_acentuacion:
    archivo: src/data/modules/acentuacion.test.ts
    hallado_en: "slice 3b, escribiendo 27-advanced-text.ts"
    problema: >
      El test rechaza `limite` siempre, exigiendo `limite` con tilde. Pero `limite`
      sin tilde es el VERBO EN SUBJUNTIVO ("un ancho que limite") y esta bien
      escrito asi; el sustantivo es el que lleva tilde. La palabra esta en la lista
      CON_FLEXION, que el archivo describe como "inequivocas por diccionario", y no
      lo es. Es la misma clase de caso dependiente de contexto que el propio
      docstring excluye a proposito con `esta` y `solo`.
    alcance: >
      Mismo falso positivo con `habilite`, `deposite`, `milite` y cualquier otro
      subjuntivo homografo. `habilite` importa en particular: aparece seguido al
      hablar de habilitar modulos por cohorte.
    estado: >
      NO se toco. La frase de 3b se reformulo para no ampliar el alcance del slice.
      Reportado al instructor, pendiente de su decision. Opciones: sacar la palabra
      de la lista, o exigir la tilde solo cuando viene precedida de articulo
      (`el limite`, `un limite`, `su limite`).
    slice_3c_attribute_selectors:
      status: done
      branch: feat/css-attribute-selectors
      merged: true
      gates: "173 tests, tsc, lint 0 errores, build, 5 E2E -- los CINCO verdes"
      changed_lines: 540
      changed_paths: 18
      rollout_pendiente: >
        AGREGA EL SLUG `attribute-selectors`. merge -> redeploy -> habilitar por
        cohorte en /teacher/modulos.
      bounded_review:
        lineage: review-1a6d5cb250fd6cd7
        risk: medium
        inspection: "completed, 18 de 18 rutas"
        findings: "1 WARNING (icon) + 1 SUGGESTION (cobertura), ninguno bloqueante"
        gates: "pre-commit allow, pre-pr allow, pre-push allow"
      ledger_measurements: { before: 23, after: 23, ceiling: 23, aporte_del_modulo_nuevo: 0 }
      hallazgo_icon: >
        El revisor acerto en el HECHO y erro en la CONSECUENCIA, y las dos quedaron
        verificadas. `SquareBrackets` NO es export de lucide-react. Pero el campo
        `icon` de un modulo NO SE RENDERIZA en ninguna parte: ICON_MAP esta indexado
        por `NavItem.icon` y el `typeInfo.icon` del detalle de modulo es el icono del
        TIPO DE EJERCICIO. Ni /modulos ni /teacher/modulos leen `module.icon`.
        Corrobora el diseno D5. Y 56 modulos ya llevan tokens invalidos, la mayoria
        kebab-case, sin romper nada. Corregido a `Brackets` en commit propio.
      desviacion_declarada_escenario_3_1: >
        La leccion 01 contiene comilla simple y valor sin comillas, en el bloque que
        muestra las tres formas lado a lado. Es el bloque que ENSENA la trampa, asi
        que esconderlo iria contra el proposito del requisito. Cada linea lleva
        comentario inline. Todos los targetCSS usan comilla doble sin excepcion.

  defecto_abierto_del_grader_de_selectores:
    archivo: src/lib/cssRules.ts
    funcion: normalizarSelectores
    hallado_en: "slice 3c, midiendo con el grader antes de escribir el modulo"
    medicion: >
      Sobre un target de dos reglas con selectores de atributo:
      comilla doble 100, comilla simple 0, sin comillas 0. CERO, no 50, porque las
      dos reglas fallan a la vez.
    problema: >
      `[href^=https]` sin comillas ES CSS VALIDO -- las comillas son opcionales
      cuando el valor es un identificador. Y `[href^='https']` con comilla simple
      tambien. El grader puntua en 0 codigo correcto, porque normalizarSelectores
      solo baja a minusculas y colapsa espacios: no normaliza el estilo de comilla.
    alcance_medido: >
      Exactamente 3 ejercicios en todo el repositorio, y los tres son los que agrega
      el slice 3c (28-ej-05, 28-ej-07, 28-ej-08). NADA en produccion esta afectado:
      ni math-functions ni advanced-text tienen valores entre comillas en un selector.
    por_que_no_se_arreglo_aca: >
      normalizarSelectores respalda TODO ejercicio css-rules de TODOS los tracks.
      Tocarla en medio de la cadena moveria los baselines medidos (ledger 23,
      sinEscribir 63) de los que 3d y 3e todavia dependen, y el arreglo necesita su
      propio test que fije las tres formas como equivalentes.
    arreglo_propuesto: >
      Normalizar el estilo de comilla Y la forma sin comillas dentro del corchete,
      mas un test que pine las tres formas como equivalentes. Cuando eso entre, el
      requisito 3 de la spec (fijar comilla doble) queda innecesario hacia adelante,
      aunque se mantiene como spec congelada de este cambio.
    estado: "pendiente, despues de archivar css-track-expansion"
    slice_3d_lists_and_tables:
      status: done
      branch: feat/css-lists-and-tables
      merged: true
      gates: "173 tests, tsc, lint 0 errores, build, 5 E2E -- los CINCO verdes"
      changed_lines: 531
      changed_paths: 12
      rollout_pendiente: >
        AGREGA EL SLUG `lists-and-tables`. merge -> redeploy -> habilitar por cohorte.
      bounded_review:
        lineage: review-b7c14679638d2d80
        risk: medium
        inspection: "completed, 12 de 12 rutas"
        findings: "1 WARNING deterministico, un defecto REAL que le pegaba al alumno"
        gates: "pre-commit allow, pre-pr allow, pre-push allow"
      ledger_measurements: { before: 23, after: 23, ceiling: 23, aporte_del_modulo_nuevo: 0 }
      hallazgo_aceptado_completo: >
        El enunciado de 29-ej-04 decia "una regla para 'datos th, .datos td'": al
        PRIMER selector le faltaba el punto de la clase. Un alumno que copia el
        enunciado literal escribe `datos th`, que apunta a una etiqueta <datos>
        inexistente, y su intento correcto se califica mal por una razon que no
        puede ver. Corregido antes del push. Barrido del repo entero buscando el
        mismo error en todo ejercicio css-rules: CERO casos mas.
      leccion_de_proceso: >
        CAMBIA UN JUICIO Y QUEDA ESCRITO. El orquestador habia argumentado DOS VECES
        por revisar solo el commit mecanico en lugar del candidato completo, sobre
        la evidencia de que cuatro revisiones no habian encontrado nada en el
        contenido de un modulo. El instructor eligio completo las cuatro veces. Este
        hallazgo es el contraejemplo: con el alcance angosto se publicaba. Para los
        slices que queden, revisar el candidato COMPLETO.
      libertad_nueva_del_ledger: >
        Desde el slot 20, `display: grid`, `display: flex`, `text-align` y
        `line-height` dejan de ser referencias adelantadas, porque flexbox (17),
        css-grid (18) y tipografias (8) quedan antes. Util para 3e (slot 22).
    slice_3e_transforms:
      status: done
      branch: feat/css-transforms
      merged: true
      merge_commit: cd3db52
      gates: "173 tests, tsc, lint 0 errores, build, 5 E2E -- los CINCO verdes"
      changed_lines: 539
      changed_paths: 11
      rollout_pendiente: >
        AGREGA EL SLUG `transforms`. merge -> redeploy -> habilitar por cohorte.
      desplazamientos: >
        OCHO modulos, no nueve. La tarea decia 9 porque el forecast del diseno se
        escribio contra 25 modulos y quedo viejo tras cuatro inserciones.
        transiciones-animaciones 22->23, variables-css 23->24, media-queries
        24->25, sass-fundamentos 25->26, sass-avanzado 26->27, bootstrap-5 27->28,
        tailwind-css 28->29, proyecto-cv-css 29->30.
      bounded_review:
        lineage: review-bd3b06c13ad3ca4e
        risk: medium
        lens: review-reliability
        inspection: "completed, 11 de 11 rutas"
        findings: "1 WARNING inferencial, cero blockers"
        gates: "pre-commit allow, pre-pr allow, pre-push allow"
      ledger_measurements: { before: 23, after: 23, ceiling: 23, aporte_del_modulo_nuevo: 0 }
      sin_escribir_measurements: { before: 63, after: 63, ceiling: 63 }
      por_que_el_aporte_tenia_que_ser_cero: >
        El ledger NO tenia aire. `shadows-gradients-filters` (21) ya gastaba un
        caso apuntando a `transiciones-animaciones`, y al mover ese destino de 22
        a 23 el caso SIGUE contando, porque shadows sigue estando antes. O sea que
        los 23 quedaron intactos por si solos y el modulo nuevo tenia que aportar
        cero exacto, no "poco".
      prohibidos_medidos_en_el_slot_22: >
        Las cuatro que el ledger cobra desde el slot 22 son `transition:`,
        `@container`, `@supports` y `@media` con width/height/orientation. Las
        cuatro apuntan a destinos posteriores (transiciones 23, media-queries 25).
        `var(--` y `@keyframes` NO estan en el ledger: solo aparecen en el guard de
        ejercicios, que tolera saltos de hasta 3 modulos, asi que a 22 salian
        gratis. Se evitaron igual, por honestidad pedagogica.
      el_guard_del_slice_2_tiene_dientes: >
        Medido, no asumido: quitando a mano la entrada "transforms" del array
        SECUENCIA fallan las TRES formas a la vez -- secuencia exacta, order igual
        a indice mas uno, y agrupar por seccion. No solo el largo. La reescritura
        del slice 2 se cobro su valor justo en el ultimo slice, que es el primero
        que cierra 1..30.
      deuda_conocida_y_aceptada: >
        El WARNING de la revision: en el literal de ALL_MODULES `transformsModule`
        quedo antes de `shadowsGradientsFiltersModule`, o sea order 22 antes de
        order 21. Medido: el bloque CSS del literal tenia CERO roturas de
        monotonia y esta es la unica; js y react estan en 0, html tiene una
        preexistente. SIN efecto de comportamiento, porque el literal termina en
        `.sort((a, b) => a.order - b.order)`. Se arreglo, se midio en cero, y se
        REVIRTIO a pedido del instructor: corregirlo despues del recibo exigia
        reabrir un linaje aprobado con autorizacion de mantenedor, que es
        maquinaria desproporcionada para mover un identificador. Va junto con el
        renombre de archivos, que es la misma clase de desalineacion.
      leccion_de_proceso: >
        DOS ERRORES DEL ORQUESTADOR, los dos corregidos antes del recibo.
        Primero: un `git add -A` metio al commit seis archivos ajenos
        (.claude/skills/interface-design/, duplicado del .agents/ ya trackeado, y
        un .gitattributes con `* text=auto eol=lf` que puede renormalizar finales
        de linea de todo el repo). Se reescribio el commit y el candidato volvio a
        11 rutas; los dos ajenos quedaron sin trackear, intactos.
        Segundo: al preguntar si arreglar el WARNING, se cotizo el costo como
        "otro consentimiento y otra corrida del revisor". Era mas caro: con el
        recibo ya emitido, cambiar el candidato pide `external.authorize_recovery`
        con disposicion `scope_changed`, o sea autorizacion de MANTENEDOR. Cotizar
        de menos un costo antes de una decision es un error propio, no un detalle.
    estado_al_cerrar_el_slice_3e:
      fecha: 2026-08-23
      main: cd3db52
      pusheado: false
      track_css: "30 modulos, 1..30 sin huecos ni duplicados"
      slices_hechos: [1, 2, "3a", "3b", "3c", "3d", "3e"]
      slices_pendientes: []
      rollout_confirmado_por_el_instructor:
        - "math-functions: redeploy + habilitado por cohorte"
        - "advanced-text: redeploy + habilitado por cohorte"
        - "attribute-selectors: redeploy + habilitado en AMBAS cohortes"
        - "lists-and-tables: redeploy + habilitado por cohorte"
      rollout_pendiente:
        - "transforms: mergeado a main, SIN pushear, SIN desplegar y SIN habilitar"
      contenido_validado_por_el_instructor:
        - "math-functions: revisado, aprobado ('esta muy bien')"
        - "attribute-selectors: revisado, aprobado"
        - "advanced-text: revisado, aprobado"
        - "lists-and-tables: revisado, aprobado"
        - "transforms: NO revisado todavia"
      baselines_finales:
        ledger: "23, techo 23, CERO margen"
        sin_escribir: "63, techo 63, CERO margen"
      linajes_de_revision:
        aprobados: 7
        abandonado: "review-c329f7b19492b56e (el candidato de 33 rutas que excedia el limite de 32 del contexto del revisor; quedo abierto e inservible, no se toco)"
      decision_de_revision_fijada: >
        Revisar el CANDIDATO COMPLETO, no solo el commit mecanico. El orquestador
        habia argumentado dos veces lo contrario y el slice 3d lo refuto: el
        revisor encontro un enunciado que pedia un selector sin el punto de la
        clase, que hacia fallar un intento correcto. Con el alcance angosto se
        publicaba.
  verify:
    status: done
    engram_topic_key: sdd/css-track-expansion/verify-report
    engram_observation: 282
    resultado: "PASS WITH WARNINGS"
    gates_reverificados_por_el_verifier: >
      Los cinco corridos de nuevo por el verificador, no heredados: 173 tests,
      tsc limpio, lint 0 errores / 49 warnings preexistentes, build, 5 E2E.
    mediciones_re_derivadas: >
      El verificador NO acepto las mediciones del orquestador: re-implemento la
      logica exacta del ledger y del filtro de sinEscribir en scripts propios
      contra los datos vivos, y obtuvo 23 y 63. Tambien re-derivo la secuencia
      1..30 leyendo los 30 archivos de modulo por shell en lugar del test, y
      confirmo por su cuenta que el 3e desplaza OCHO modulos y no nueve.
    requirement_4_confirmada: >
      30 modulos, order 1..30 exacto, cero huecos, cero duplicados de order y de
      slug, y agrupar por DOJO_CATEGORY_ORDER.css reproduce el mismo recorrido.
      Es la unica que solo se podia cerrar en el ultimo slice, y cerro limpia: no
      habia deuda torcida de los seis slices anteriores.
    hallazgo_critical_y_su_arreglo:
      que_paso: >
        El Requirement 8 de css-track-content decia que un modulo bloqueado
        "SHALL be invisible" y el Scenario 8.1 decia "MUST NOT appear". Las dos
        afirmaciones eran FALSAS sobre el comportamiento que se despliega:
        src/app/(app)/modulos/page.tsx renderiza el modulo bloqueado como tarjeta
        gris no clickeable cuando `!isEnabled && !isTeacher`.
      por_que_importaba: >
        design.md D7 YA tenia la correccion escrita desde la fase de diseno, pero
        el texto de la spec nunca se redlineo. Y archive fusiona las delta specs
        dentro de las specs principales, asi que archivar sin corregir promovia
        esa afirmacion falsa a la spec durable del proyecto, donde sobrevive al
        cambio que la genero.
      arreglo: >
        Enmendado el Requirement 8 y el Scenario 8.1 para describir el
        comportamiento real, con una nota que dice explicitamente que el texto se
        corrigio despues de implementar y por que. Agregado el Scenario 8.2, que
        deja escrita la brecha de cobertura: NINGUN test cubre ni la tarjeta con
        candado ni su ausencia. Verificado por el orquestador con busqueda propia:
        cero tests mencionan enabledSlugs, cursor-not-allowed o isTeacher, y los
        dos E2E son el harness del worker y la landing. Cerrar esa brecha queda
        fuera de alcance de este cambio.
    deuda_de_comentario_encontrada_y_no_arreglada: >
      src/data/modules/index.ts:130-132. El comentario esta mal escrito ("este
      array se ordena por el / al final del archivo", falta el sustantivo) Y dice
      algo falso: afirma que el orden de declaracion "tiene que coincidir" y que
      orden-curriculum-css.test.ts falla si se desincronizan. NO falla: el test
      lee el array ya ordenado, no el literal. Esto agrava la deuda aceptada del
      WARNING del 3e, porque el literal ahora contradice al comentario de tres
      lineas mas arriba. Es un archivo .ts, asi que tocarlo dispara revision
      completa con consentimiento; va junto con el renombre de archivos.
  archive:
    status: pending
    bloqueado_por: >
      La tarea 3e.5, que es del instructor y no la puede cerrar ningun agente:
      pushear main, redeploy manual en Coolify, y habilitar el slug `transforms`
      por cohorte en /teacher/modulos. En ese orden, que no se puede invertir.

engram_summary_observation: 260   # "SDD css-track-expansion: planificacion completa" (decision)

next_recommended: archive   # verify done (PASS WITH WARNINGS); bloqueado por la tarea 3e.5 del instructor

deferred_work:
  - "Fase 2 — web typography (Google Fonts, @font-face), overflow, inheritance (inherit/initial/unset/revert), images and media (object-fit, clip-path)."
  - "Fase 3 — responsive images and video, visual accessibility, debugging with DevTools. The last two are our differentiator; W3Schools treats them as an appendix."
  - >
    Fase 4 — interleaved challenges (one small graded challenge after each
    lesson instead of a block at the end). This is the ONE thing worth copying
    from W3Schools, and the only item that improves the 25 EXISTING modules
    without writing new content. It needs a schema change: the `Exercise`
    interface in src/types/index.ts has NO link to lessons (only `order`), so it
    needs an optional field such as `afterLesson`, plus renderer support.
    Backwards compatible: without the field, everything behaves as today.

open_questions_not_decided:
  - >
    37 modules may be too many for one cohort. Distinguishing REQUIRED modules
    from optional deepening has no representation in the data model today. The
    instructor flagged it as an open question; no phase invented an answer, and
    none should.
  - >
    How much of this reaches the in-flight Cohorte 2 (whose CSS block is classes
    6-14) versus the next one. The new modules are self-study and reinforcement,
    not new class sessions.
  - >
    File numbering already drifts from track order (11-box-model.ts carries
    order: 4). This change leaves the filenames alone deliberately; renaming
    files is a separate, mechanical change to do in one go with the guard green.
